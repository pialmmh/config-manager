# RouteSphere Global Configuration
# This file contains ONLY the active tenant and profile selection
# All other configurations are in tenant-specific files

routesphere:
  global:
    # Active tenant name (e.g., telcobright_root, ccl, reseller_premium)
    tenant: ccl
    
    # Active profile name (e.g., dev, staging, prod, mock)
    profile: dev